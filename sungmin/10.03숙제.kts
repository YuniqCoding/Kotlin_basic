 2754
fun main() {
    val grade = readLine()!!
    var point = 0.0

    when(grade) {
        "A+" -> point = 4.3
        "A0" -> point = 4.0
        "A-" -> point = 3.7
        "B+" -> point = 3.3
        "B0" -> point = 3.0
        "B-" -> point = 2.7
        "C+" -> point = 2.3
        "C0" -> point = 2.0
        "C-" -> point = 1.7
        "D+" -> point = 1.3
        "D0" -> point = 1.0
        "D-" -> point = 0.7
        "F" -> point = 0.0
    }
    println(point)
}
main()

 2738
fun main() {
val N = readLine()

}

25372
fun main() {
    val N = readLine()!!.toInt()

    for (i in 1..N) {
        var a = readLine()!!.length
    }
    if (a >= 6 && a <= 9) println("yes")
    if (b < 6 || b > 9) println("no")
    if (c < 6 || c > 9) println("no")
}
main()   이 문제는 풀어보고 있습니다.

4999
fun main() {
    var a = readLine()!!.length // 재환이가 낼 수 있는 ah의 길이
    var b = readLine()!!.length //의사가 원하는 ah의 길이

    if(a >= b) println("go")
    else println("no")


}    //정답!!!
main()

1264
fun main() {
    var a = readLine()!!
    var b = readLine()!!
    var c = readLine()!!
    var d = readLine()

//}