import kotlin.Int as Int1

//Test
//1. 학생의 시험 점수를 넣어주면 학점을 반환하는 함수를 만드시오
       // (90점 이상 A학점, 80~89점은 B학점, 70~79점은 C학점, 그렇지 않으면 F학점)
  //시험 점수 : num01
//val num01 : Int =  85
//fun TestScore( num01 : Int ): Int {
//     if (num01 >= 90) {
//        println("A학점입니다.")
//    } else if (num01 >= 80) {
//        println("B학점입니다.")
//    } else if (num01 >= 70) {
//        println("C학점입니다.")
//    } else {
//        println("F학점입니다.")
//
//
//    }
//return num01
//}
//
// println(TestScore(num01))

//2. 변수 A와 B를 선언하고, 두 변수의 값이 같은 경우에는 true, 그렇지 않은 경우에는 false가 되는 변수 c를 선언한다
       // 단 변수 A와 B의 자료형은 자유이지만 에러가 발생하면 안된다.

//val A : String = "True"
//val B : String = "False"
//val C = if(A == B) {
  //  println("True")

//}   else{

  //  println("False")
//}
//println(C)


//3. nullable 정수형 두개를 받는 함수를 만든다. 이 함수는 받은 인수의 합을 반환한다
//이때 인수중에 null 이 있으면 0으로 취급하여 합을 구한다.

var a : Int? = null
var b : Int? = null
fun PlusNumber(a : Int , b : Int ): Int {


     }
return c
 println(PlusNumber(a + b)

//잘 모르겠습니다 ㅠ

